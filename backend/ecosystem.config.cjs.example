module.exports = {
  apps: [
    {
      name: 'examiner-backend',
      script: './dist/server.js',
      cwd: '/home/sudoberry/projects/examiner/backend',
      interpreter: 'node',
      autorestart: true,
      watch: false,
      max_memory_restart: '300M',
      out_file: './logs/out.log',
      error_file: './logs/error.log',
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
      merge_logs: true,

      env_production: {
        NODE_ENV: 'production',
        PORT: 8031,
        TRUST_PROXY: 'true',
        FRONTEND_URLS: 'https://REPLACE_ME.com',

        // ── Authentifizierung ────────────────────────────────────────────────────
        // node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
        JWT_SECRET: 'REPLACE_WITH_RANDOM_64_CHAR_HEX',

        // ── KI-Anbieter ──────────────────────────────────────────────────────
        // Nur EINEN Key setzen — der erste gefundene wird als Server-Fallback
        // genutzt. User-Keys (über die Einstellungen gesetzt) haben immer Vorrang.
        //
        // OpenAI:     https://platform.openai.com/api-keys
        // Anthropic:  https://console.anthropic.com/settings/keys
        // Google:     https://aistudio.google.com/apikey
        // Mistral:    https://console.mistral.ai/api-keys
        OPENAI_API_KEY: 'sk-REPLACE_ME',
        // ANTHROPIC_API_KEY: 'sk-ant-REPLACE_ME',
        // GOOGLE_API_KEY:    'AIza-REPLACE_ME',
        // MISTRAL_API_KEY:   'REPLACE_ME',

        // Verschlüsselungsgeheimnis für gespeicherte User-API-Keys (AES-256-GCM).
        // Langen zufälligen String verwenden:
        //   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
        // WICHTIG: Bei Änderung werden alle gespeicherten User-Keys ungültig!
        API_KEY_SECRET: 'REPLACE_WITH_RANDOM_64_CHAR_HEX',
      },

      env_development: {
        NODE_ENV: 'development',
        PORT: 8031,
        TRUST_PROXY: 'false',
        FRONTEND_URLS: 'http://localhost:8030,http://localhost:5173',

        JWT_SECRET: 'REPLACE_WITH_RANDOM_64_CHAR_HEX',

        // Nur einen Key setzen reicht für den Betrieb
        OPENAI_API_KEY: 'sk-REPLACE_ME',
        // ANTHROPIC_API_KEY: 'sk-ant-REPLACE_ME',
        // GOOGLE_API_KEY:    'AIza-REPLACE_ME',
        // MISTRAL_API_KEY:   'REPLACE_ME',

        API_KEY_SECRET: 'REPLACE_WITH_RANDOM_64_CHAR_HEX',
      },
    },
  ],
}
